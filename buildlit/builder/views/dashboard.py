from django.db import models

# Create your models here.

# @api_view(['GET'])
# @permission_classes([IsAuthenticated])
# def builder_dashboard(request):
    # 1. Verify role == builder
    # 2. Get StartupProfile
    # 3. Get StartupTeam
    # 4. Get builder’s posts (from posts app)
    # 5. Get top joiners (from joiner/leaderboard app)
    # 6. Get competitions created (from competitions app)

    # 7. Return everything as JSON
